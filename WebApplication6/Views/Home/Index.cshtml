@using WebApplication6.Models;
@{
    Layout = "_Layout";
}
@model List<HrManagement>

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">HrManagement id</th>
            <th scope="col"> HrManagement Name</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count; i++)
        {
            <tr>
                <th scope="row">@i</th>
                <td>@Model[i].Id</td>
                <td><a asp-controller="Department" asp-action="index" asp-route-id="@Model[i].Id" class="text-decoration-none text-black">@Model[i].Name</a></td>
            </tr>
        }
    </tbody>
</table>
